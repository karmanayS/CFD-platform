services:
  redis:
    image: redis
    ports: 6379:6379

  poller:
    build:
      context: ./poller
      dockerfile: dockerfile
    
  ws:
    build:
      context: ./ws-server
      dockerfile: dockerfile
    ports: 8080:8080
  engine:
    build:
      context: ./engine
      dockerfile: Dockerfile

  api:
    build:
      context: ./http-server
      dockerfile: dockerfile
    ports: 3000:3000
